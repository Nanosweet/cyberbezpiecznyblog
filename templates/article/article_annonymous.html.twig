{% extends 'base.html.twig' %}

{% block title %}tytul art {# article.title #} {% endblock %}

{% block body %}

<body>
  <header class="masthead">
    <div class="overlay"></div>
    <div class="container">
      <div class="row">
        <div class="col">
          <div class="post-heading">
            <h1>{{ article.title }}</h1>
            <h2 class="subheading">podtytul art{# article.subtitle #}</h2>
            <span class="meta">Dodał(a)
              <a href="#">imie naziwsko autora{# article.author.imie #} {# article.author.nazwisko #}</a>
              data publikacji{# article.publishedAt | date('d.m.Y')#}
            </span>
          </div>
        </div>
      </div>
    </div>
  </header>
  <!-- Article Content -->
  <article>
    <div class="container">
      <div class="row">
        <div class="col">
         kontent {# article.content #}
        </div>
      </div>
    </div>
  </article>
  <!-- Sekcja komentarzy -->
  {% include 'article_comments/article_comments.html.twig' %}
</body>

{% endblock %}
